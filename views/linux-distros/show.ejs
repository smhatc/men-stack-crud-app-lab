<!DOCTYPE html>
<html lang="en">

<head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title><%= foundLinuxDistro.name %> | <%= title %></title>
</head>

<body>
        <h1><%= foundLinuxDistro.name %></h1>
        <p><strong>Description:</strong>
                <%= foundLinuxDistro.description %>
        </p>
        <p><strong>Base Distro:</strong>
                <%= foundLinuxDistro.baseDistro %>
        </p>
        <p><strong>Desktop Environment:</strong>
                <%= foundLinuxDistro.desktopEnvironment %>
        </p>
        <div>
                <form action="/linux-distros/<%= foundLinuxDistro._id %>?_method=DELETE" method="post">
                        <button type="submit">Delete <%= foundLinuxDistro.name %></button>
                </form>
                <a href="/linux-distros/<%= foundLinuxDistro._id %>/edit">Edit <%= foundLinuxDistro.name %></a>
        </div>
        <a href="/linux-distros">Back to All Businesses</a>
</body>

</html>